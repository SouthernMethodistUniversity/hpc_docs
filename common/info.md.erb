<%-
require 'open3'
begin
    script = 'cat ' + staged_root.to_s + '/warnings.txt'
    o, status = Open3.capture2('bash', stdin_data: script)
    warnings = o.split("\n")
end
-%>

<%- warnings.each do |q| -%>
### Warning:

> <%= q %>

---

<%- end -%>

#### **Do not leave idle sessions open**: We may terminate your job to free up the resources for other users!